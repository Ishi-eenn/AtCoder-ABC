#include <stdio.h>
#define N 200000

int main(void){
	int n, ans = 0, p[N], s[N];

	scanf("%d", &n);
	for(int i = 0; i < n; i++){
		scanf("%d", &p[i]);
	}

	for(int i = 0; i < n; i++){
	}
	return (0);
}